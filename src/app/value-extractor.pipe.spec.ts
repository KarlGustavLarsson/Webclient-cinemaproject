import { ValueExtractorPipe } from './value-extractor.pipe';

describe('KeyExtractorPipe', () => {
  it('create an instance', () => {
    const pipe = new ValueExtractorPipe();
    expect(pipe).toBeTruthy();
  });
});
